<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:gravity="center"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:orientation="vertical"
    android:background="@color/light_yellow">

    <TextView
        android:id="@+id/valve_status"
        android:layout_marginTop="40dp"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:textSize="26sp"
        android:textColor="@color/facebook" />

    <TextView
        android:id="@+id/sensor_status"
        android:layout_marginTop="40dp"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:textSize="26sp"
        android:textColor="@color/facebook" />

    <Button
        android:id="@+id/switch_valve"
        android:layout_marginTop="40dp"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:text="@string/switch_valve"
        android:onClick="switchValve"
        tools:ignore="UsingOnClickInXml"
        android:textSize="26sp"
        android:textColor="@color/facebook"
        android:backgroundTint="@color/dark_yellow"/>

    <Button
        android:id="@+id/send_config"
        android:layout_marginTop="40dp"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:text="@string/send_config"
        android:onClick="setConfig"
        tools:ignore="UsingOnClickInXml"
        android:textSize="16sp"
        android:textColor="@color/facebook"
        android:backgroundTint="@color/dark_yellow"/>

</LinearLayout>